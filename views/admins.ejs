<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <title>admins</title>
    <style>
        .admin {
            border: 1px solid #ccc;
            border-radius: 8px;
            padding: 10px;
            margin: 10px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            width: 230px; /* Adjust the width as needed */
        }

        /* Add any additional styling for the .admin class here */
    </style>
</head>
<body>
    <div style="width:50%; margin:50px auto">
        <h2 style="text-align:center;color:darkred">Contact Admins</h2>
        <p style="margin-top:50px">Contact the following admins inorder to borrow or rent items that are available in our idir</p>
        <p>Note that: Anything that is not available on items page is not available in our idir</p>
    </div>
    
    <div style="display:flex; justify-content:center;align-items:center;width:100%; height:100vh">
        <% var index = 0; %>
      <% while (index < admins.length) { %>
        <div class="admin">
            <img src="images/member.png" alt="">
            <p><%= admins[index].username%></p>
            <p><%= admins[index].email%></p>
        </div>
            
      <% index++; %>
      <% } %>
    </div>
    
</body>
</html>